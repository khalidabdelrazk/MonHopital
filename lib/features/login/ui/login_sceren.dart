import 'package:flutter/material.dart';

class LoginSceren extends StatelessWidget {
  const LoginSceren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Login Screen"
        ),
      ),
    );
  }
}